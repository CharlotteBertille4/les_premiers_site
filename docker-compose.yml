version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lespremiers_site_app
    ports:
      - "8080:8000"
    volumes:
      - ./:/lespremiers_site
    working_dir: /lespremiers_site
    command: php -S 0.0.0.0:8000 -t public
